#!/bin/sh
if [ -z $1 ]; then
        echo "update_preloader <tftp_dir>"
        exit
fi
if [ -z $global.dhcp.tftp_server_name ]; then
        dhcp
fi
echo updating bootloader
barebox_update -t qspi-xload -y $1/barebox-xload-image
