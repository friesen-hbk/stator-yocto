# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-hbk-distro"
BBFILE_PATTERN_meta-hbk-distro = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-hbk-distro = "6"

LAYERDEPENDS_meta-hbk-distro = "core"
LAYERSERIES_COMPAT_meta-hbk-distro = "warrior zeus"
